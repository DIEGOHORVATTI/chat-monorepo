{
  "name": "@repo/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/router.ts"
    }
  },
  "dependencies": {
    "@orpc/contract": "catalog:",
    "@orpc/openapi": "^1.8.4",
    "@orpc/server": "catalog:",
    "@orpc/zod": "^1.8.4",
    "@prisma/client": "^7.0.1",
    "@react-email/render": "^2.0.0",
    "@repo/contracts": "catalog:",
    "@repo/emails": "catalog:",
    "@repo/service-core": "catalog:",
    "bcrypt": "^6.0.0",
    "nodemailer": "^7.0.5",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/eslint": "workspace:*",
    "@repo/typescript": "workspace:*",
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^24.3.0",
    "@types/nodemailer": "^7.0.1",
    "@vitest/ui": "^4.0.14",
    "bun-types": "latest",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "prisma": "^7.0.1",
    "vitest": "^4.0.14"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --target=bun --compile --outfile dist/bin",
    "start": "bun dist/index.js",
    "prisma:studio": "bun prisma studio",
    "prisma:generate": "bunx prisma generate",
    "lint": "eslint --max-warnings 0",
    "prettier:check": "bunx prettier --write \"src/**/*.{js,jsx,ts,tsx}\"",
    "lint:fix:backend": "eslint --max-warnings 0 --fix",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "type-check": "tsc --noEmit"
  }
}
